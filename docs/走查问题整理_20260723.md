# 走查问题整理（2026-07-23 首轮实测）

> **来源**：[端到端演示走查清单](./端到端演示走查清单.md) §8 问题记录 + 未勾选项
> **状态**：全部 6 个报告问题已定位根因，其中 5 个已修复并验证（前端 build ✓ / 后端 279 pytest ✓），1 个为功能缺失排期处理
> **修复代码**：见本节末尾 commit 记录

## 一、问题 → 根因 → 处置 总表

| # | 你记录的现象 | 定位根因 | 性质 | 处置 |
|---|---|---|---|---|
| 1 | 场景包管理：无数据 | 后端列表返回 `report_packs`，前端读 `.packs` → undefined | 🔴 契约不一致 | ✅ 已修（前端 3 处读取点改 `report_packs`） |
| 2 | 新建任务：场景包加载失败 `...reading 'filter'` | 同 #1，`undefined.filter` 直接崩弹窗 | 🔴 同上 | ✅ 已修（同源） |
| 3 | 映射工作台：无对应任务 | 被 #2 阻断——任务没建成，自然无映射可看；另发现清单缺 `caliber_text`/`candidates` 两字段 | 🔴 阻断性 + 🟡 字段缺失 | ✅ 已修（#2 同源 + 后端补字段） |
| 4 | 六维校验+勾稽：无页面 | 六维页面**存在**（任务大厅"校验报告"按钮进入），但侧边栏无入口；勾稽结果**确实无前端展示**（功能缺失） | 🟡 入口深 + 功能缺失 | ⏳ 勾稽展示排入 P2（见第四节） |
| 5 | 制度预演：点击后空白页 | diff 接口 `affected_keywords` 返回对象数组 `[{keyword,section}]`，前端当 string 渲染 → React 崩溃白屏（无 ErrorBoundary） | 🔴 契约不一致 | ✅ 已修（前端归一化取 keyword） |
| 6 | 接口导出：页面错乱 | preview 返回 `string[]` 被当 string 渲染，内容挤成一行 | 🟡 契约不一致 | ✅ 已修（渲染前 join） |

**未勾选项归因**：3.2/3.3/3.4/3.5/3.7 全部被 #1~#5 连锁阻断，非独立问题；4.1（断点恢复）、4.3（取消任务）、5.1（SQL 红线）为未测项，列入下轮走查。

## 二、修复明细（本轮已落地）

### 契约不一致类（同根因：后端下发复合结构，前端按标量消费）

| 修复点 | 文件 | 方式 |
|---|---|---|
| 场景包列表 `report_packs` vs `.packs` | client.ts + TaskHall/DigitalTwin/ReportPacks 三页 | 前端读取点统一改 `report_packs` |
| 相似任务 `summary` 是 dict 当 string 渲染 → 新建任务弹窗白屏 | TaskHall.tsx:19-38 | 新增 `similarSummaryText()` 归一化 |
| diff `affected_keywords` 对象数组 → 制度预演白屏 | DigitalTwin.tsx:17-20 | 新增 `keywordText()` 归一化 |
| 导出 `preview` 数组挤一行 | TaskExecute.tsx:175-178 | 渲染前 `join('\n')` |
| createTask 返回类型过宽 | client.ts:268-275 | 收窄为 `CreateTaskResult` |

### 字段缺失类（后端补齐）

| 修复点 | 文件 | 方式 |
|---|---|---|
| 映射清单缺口径文本/候选列表 | backend/api/mappings.py | 口径取自场景包 target_schema；候选 best-effort 重建（降级为空不阻断） |
| 导出列表缺行数/时间 | backend/services/interface_file_service.py | 生成时写 sidecar `.meta.json` 存 row_count；created_at 取 mtime |

### 页面逻辑类

| 修复点 | 文件 | 方式 |
|---|---|---|
| 场景包详情抽屉全空 / admin 编辑白屏 | ReportPacks.tsx | 详情/编辑改调 `getReportPack` 拉完整包（原闲置封装），带 loading 态；移除恒为 0 的"字段数"列 |

## 三、验证

- 前端 `npm run build` ✓、`tsc --noEmit` ✓
- 后端 `pytest tests/` **279 passed**（原 277 + 新增 2 个契约测试：映射字段补齐、导出列表 sidecar 回退）

## 四、遗留与下轮走查

| 项 | 级别 | 说明 |
|---|---|---|
| 勾稽结果前端展示 | P2 功能缺失 | 勾稽在 `outputs.test_verify` 有数据，无任何页面读取；建议在六维校验页加"勾稽对账"区块 |
| 侧边栏加"校验报告"入口 | P3 体验 | 目前只能从任务大厅按钮进入 |
| 前端无 ErrorBoundary | P3 健壮性 | 任一渲染异常即整页白屏；建议全局包一层，错误时显示组件级错误而非空白 |
| 4.1 断点恢复 / 4.3 取消任务 / 5.1 SQL 红线 | 未测 | 下轮走查重点 |
| mapping-assets 资产库 API 无页面调用 | P3 死功能 | 后续映射资产库功能启用时接入 |

**给你的复测提示**：你本机 8080 服务带 `--reload`，后端改动已自动生效；前端需 `git pull` 后等 Vite 热更（或刷新页面）。建议从 3.2 场景包管理开始按原清单复测 3.2→3.7 主线。
