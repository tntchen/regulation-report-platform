# 核心框架
fastapi==0.111.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# 数据库
sqlalchemy==2.0.31
aiosqlite==0.20.0
PyMySQL==1.1.1

# 向量库(轻量版)
faiss-cpu==1.8.0
numpy==1.26.4

# AI客户端(OpenAI兼容)
openai==1.35.0
httpx==0.27.0

# 文档解析
PyPDF2==3.0.1
python-docx==1.1.2

# 工具
pydantic==2.7.4
pydantic-settings==2.3.4
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4 与 bcrypt>=4.1 不兼容（__about__ 移除 + 72 字节自检报错），必须锁 4.0.1
python-dotenv==1.0.1

# 异步
aiofiles==23.2.1
greenlet==3.0.3

# 开发调试
pytest==8.2.2
pytest-asyncio==0.23.7

# SQL 只读纵深（L2-D6）
sqlglot>=26.0.0

# 语义向量检索（L2-D8）：local provider 默认路径
# 注意：torch 体积较大（CPU 版约 200MB+）；不装时 embedding_provider 可切 tfidf 兜底，
# 或配 remote（OpenAI 兼容端点）走外部服务。BGE 模型首次运行需联网下载（HF 缓存后离线可用）。
sentence-transformers>=5.0.0
scikit-learn>=1.4.0

# MySQL 生产路径（可选：Docker 环境启用真实链路时安装）
# asyncmy>=0.2.9
# pymysql>=1.1.0  # 种子脚本 scripts/seed_mysql.py 使用
